<%= form_with(model: rol_usuario, local: true) do |form| %>
  <% if rol_usuario.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(rol_usuario.errors.count, "error") %> prohibited this rol_usuario from being saved:</h2>

      <ul>
      <% rol_usuario.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :usuario_id %>
    <%= form.number_field :usuario_id %>
  </div>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
