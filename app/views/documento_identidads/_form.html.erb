<%= form_with(model: documento_identidad, local: true) do |form| %>
  <% if documento_identidad.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(documento_identidad.errors.count, "error") %> prohibited this documento_identidad from being saved:</h2>

      <ul>
      <% documento_identidad.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
