<%= form_with(model: privilegio_empleado, local: true) do |form| %>
  <% if privilegio_empleado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(privilegio_empleado.errors.count, "error") %> prohibited this privilegio_empleado from being saved:</h2>

      <ul>
      <% privilegio_empleado.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_field :descripcion %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
