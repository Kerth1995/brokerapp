<div class="row">
  <div class="col-md-6">
    <%= form_with(model: rol, local: true) do |form| %>
      <% if rol.errors.any? %>
        <div id="error_explanation">
          <h6><%= pluralize(rol.errors.count, "error") %> prohibited this rol from being saved:</h6>

          <ul>
            <% rol.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="form-group row">
        <%= form.label :codigo, class: 'col-sm-2 col-form-label' %>
        <div class="col-md-4">
          <%= form.text_field :codigo, class: 'form-control form-control-sm' %>
        </div>

      </div>

      <div class="form-group row">
        <%= form.label :nombre, class: 'col-sm-2 col-form-label' %>
        <div class="col-md-10">
          <%= form.text_field :nombre, class: 'form-control form-control-sm' %>
        </div>

      </div>

      <div class="form-group row">
        <%= form.submit class: 'btn btn-primary btn-sm' %>
      </div>
    <% end %>
  </div>
  <div class="col-md-6">

  </div>
</div>



